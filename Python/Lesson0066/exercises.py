import inspect

def list_public_methods(cls):
    """Bài 1: Viết hàm liệt kê các phương thức công khai của lớp.
    Trả về danh sách tên phương thức (chuỗi) không bắt đầu bằng '_'.
    """
    pass


def get_function_defaults(func):
    """Bài 2: Trả về từ điển chứa tên tham số và giá trị mặc định.
    Nếu tham số không có giá trị mặc định, không đưa vào kết quả.
    """
    pass


def debug_caller():
    """Bài 3: In ra tên hàm gọi nó và tên file thực thi.
    Gợi ý: dùng inspect.stack()
    """
    pass


def print_class_source(cls):
    """Bài 4: In ra mã nguồn của lớp nếu có thể.
    Nếu không, in ra thông báo lỗi.
    """
    pass


def log_call(func):
    """Bài 5: Viết decorator @log_call để in tên hàm và tham số khi gọi.
    Sử dụng inspect để lấy thông tin tham số.
    """
    pass